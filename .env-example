# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=miel_ia
DB_USER=user
DB_PASS=password

# Thresholds
THRESHOLD_BINARY=0.6
THRESHOLD_CLASSIFY=0.5

# CORS (para desarrollo usar "*", para producción especificar dominios)
ALLOWED_ORIGINS=*
ALLOWED_METHODS=*
ALLOWED_HEADERS=*
ALLOW_CREDENTIALS=true

# Security
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Logging
LOG_LEVEL=DEBUG

# Rutas de modelos ML
MODELS_PATH=trained_models
BINARY_MODELS_PATH=trained_models/binary
CLASSIFY_MODELS_PATH=trained_models/classify

# Configuración de la aplicación
APP_NAME="API de Clasificación Binaria"
APP_VERSION="1.0.0"
ENVIRONMENT=development

# Configuración del servidor
HOST=0.0.0.0
PORT=8000
DEBUG=true
WORKERS=1

# Role IDs (UUIDs)
ADMIN_ROLE_IDE=uuid-admin-role
DOCTOR_ROLE_IDE=uuid-doctor-role
PATIENT_ROLE_IDE=uuid-patient-role
TECHNICIAN_ROLE_IDE=uuid-technician-role
